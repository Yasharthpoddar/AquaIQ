# Environments
venv/
__pycache__/
*.pyc
.env

# Data — too large for git, downloaded fresh by each teammate
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Database — generated by init_db.py
db/aquaiq.db

# Model checkpoints — large binaries, track via releases or Drive instead
models/checkpoints/*.pt
models/checkpoints/*.json
!models/checkpoints/.gitkeep

# Node (dashboard, added Week 4)
node_modules/
dashboard/dist/
dashboard/build/

# OS / editor
.DS_Store
.vscode/
*.log

# MLflow
mlruns/

# Test coverage
.coverage
htmlcov/
